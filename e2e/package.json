{
  "name": "e2e",
  "version": "1.0.0",
  "description": "Playwright E2E tests for critical Inventory Supply Management flows",
  "private": true,
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "install-browsers": "playwright install"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0",
    "@types/node": "^20.11.0"
  }
}
