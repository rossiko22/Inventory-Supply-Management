apiVersion: apps/v1
kind: Deployment
metadata:
  name: product-service
  labels: { app: product-service }
spec:
  replicas: 1
  selector:
    matchLabels: { app: product-service }
  template:
    metadata:
      labels: { app: product-service }
    spec:
      containers:
        - name: product-service
          image: DOCKERHUB_USER/product-service:latest
          ports:
            - containerPort: 8085
          env:
            - name: DB_USER
              valueFrom: { secretKeyRef: { name: app-secrets, key: DB_USER } }
            - name: DB_PASSWORD
              valueFrom: { secretKeyRef: { name: app-secrets, key: DB_PASSWORD } }
            - name: ConnectionStrings__DefaultConnection
              value: "Host=postgres;Port=5432;Database=products;Username=$(DB_USER);Password=$(DB_PASSWORD)"
            - name: ASPNETCORE_URLS
              value: "http://+:8085"
          readinessProbe:
            tcpSocket: { port: 8085 }
            initialDelaySeconds: 20
            periodSeconds: 10
            failureThreshold: 30
          livenessProbe:
            tcpSocket: { port: 8085 }
            initialDelaySeconds: 60
            periodSeconds: 15
          resources:
            requests: { memory: "128Mi", cpu: "100m" }
            limits:   { memory: "384Mi", cpu: "500m" }
---
apiVersion: v1
kind: Service
metadata:
  name: product-service
  labels: { app: product-service }
spec:
  selector: { app: product-service }
  ports:
    - port: 8085
      targetPort: 8085
